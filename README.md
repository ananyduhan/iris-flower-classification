# iris-flower-classification🪻
## 🎯Goal of the project
vissually represent the data to see difference between different species,
Train differnt models with dimentions of the iris flower and then find the best model based upon their accuracy
##liberaries and languages used
python
numpy
pandas
matlotlib
seaborn
sklearn
## 🛠️How the project was build
first of all the data was imported from kaggle datasets 
seperated the dataset for training and testing 

then using pandas it was imported into the notebook
then using matplotlib and seaborn we drew some graph to see a visual difference between different classes on the bases of their shape
after graphs
i used sklearn to encode the class names into numbers(because machine do not understands english)
then comes the model training
i used different models to train and then tested them to find out which works the best
first comes LogisticRegression 
logesticregression tries to fit a line in the data which seems to be the best fit

then k neighbour classifier
as the name sugggests it finds the nearest neighbours and then when the new data comes it checks its neighbours and see which class they belong






